import { PlaceholderPage } from "@/components/placeholder-page";

export default function ResearchPage() {
  return (
    <PlaceholderPage
      eyebrow="Research"
      title="Research without the nonsense."
      body="This route will hold the evidence-backed education layer. Right now it exists so homepage visitors can see where the knowledge system will live."
      primaryHref="/"
      primaryLabel="Back to homepage"
    />
  );
}
