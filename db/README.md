# Database Notes

The canonical schema currently remains at `supabase/schema.sql`.

It was intentionally left in place because moving Supabase-managed files can interfere with future CLI or local workflow expectations.

Use this `db/` directory for future database organization only when the toolchain is updated safely.
